import './bootstrap';

// Bagian aktifkan JS Bootstrap (dropdown, modal, collapse, carrousel, dll)
import 'bootstrap';
// import * as bootstrap from 'bootstrap' (pakai kode ini untuk mengontrol komponen Bootstrap secara manual)

// Bagian auto-init komponen custom yang dibuat
